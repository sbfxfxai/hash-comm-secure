// Tests temporarily disabled during decentralization transition  
import { describe, it, expect } from 'vitest'

describe('SubscriptionService (Disabled)', () => {
  it('placeholder test', () => {
    expect(true).toBe(true)
  })
})

// TODO: Update tests for decentralized SubscriptionService interface